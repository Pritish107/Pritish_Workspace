$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
type: command

compute: azureml:ml-cpu-cluster

environment: azureml:sklearn-custom-env:1

code: ./src

command: >
  python train.py --output_dir ${{outputs.output_dir}}

outputs:
  output_dir:
    type: uri_folder










